<Application
    x:Class="SerialPortTool.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:SerialPortTool.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- MUST stay first: our dictionaries below override framework lightweight keys
                     (ControlCornerRadius, TextFillColor*, TextControl*, ComboBox*, MenuFlyout*, …),
                     and a merged dictionary only wins if it is consulted after XamlControlsResources. -->
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <ResourceDictionary Source="Themes/Tokens.xaml" />
                <ResourceDictionary Source="Themes/Controls.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Converters -->
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
            <converters:HexColorToBrushConverter x:Key="HexColorToBrushConverter"/>
        </ResourceDictionary>
    </Application.Resources>
</Application>
