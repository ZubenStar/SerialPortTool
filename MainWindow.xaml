<Window
    x:Class="SerialPortTool.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:SerialPortTool.Converters"
    mc:Ignorable="d"
    Title="{x:Bind ViewModel.Title, Mode=OneWay}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <MenuBar Grid.Row="0">
            <MenuBarItem Title="文件">
                <MenuFlyoutItem Text="退出" Click="Exit_Click"/>
            </MenuBarItem>
            <MenuBarItem Title="工具">
                <MenuFlyoutItem Text="扫描串口" Command="{x:Bind ViewModel.ScanPortsCommand}"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="打开日志文件夹" Click="OpenLogFolder_Click"/>
            </MenuBarItem>
            <MenuBarItem Title="帮助">
                <MenuFlyoutItem Text="关于" Click="About_Click"/>
            </MenuBarItem>
        </MenuBar>

        <!-- Main Content -->
        <Grid Grid.Row="1" Padding="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Grid Grid.Column="0" Padding="0,0,8,0">
                <StackPanel Spacing="16">
                    <!-- Port Configuration -->
                    <StackPanel Spacing="8">
                        <TextBlock Text="串口配置"
                                   Style="{StaticResource SubtitleTextBlockStyle}"
                                   FontWeight="SemiBold"/>
                        
                        <StackPanel Spacing="4">
                            <TextBlock Text="波特率:" FontSize="12"/>
                            <ComboBox x:Name="BaudRateComboBox"
                                      ItemsSource="{x:Bind ViewModel.AvailableBaudRates}"
                                      SelectedItem="{x:Bind ViewModel.BaudRate, Mode=TwoWay}"
                                      HorizontalAlignment="Stretch"/>
                        </StackPanel>
                        
                        <StackPanel Spacing="4">
                            <CheckBox Content="自定义波特率"
                                      IsChecked="{x:Bind ViewModel.UseCustomBaudRate, Mode=TwoWay}"
                                      FontSize="12"
                                      Checked="CustomBaudRateCheckBox_Changed"
                                      Unchecked="CustomBaudRateCheckBox_Changed"/>
                            <TextBox x:Name="CustomBaudRateTextBox"
                                     PlaceholderText="输入自定义波特率..."
                                     Text="{x:Bind ViewModel.CustomBaudRate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     HorizontalAlignment="Stretch"
                                     InputScope="Number"
                                     Visibility="Collapsed"/>
                        </StackPanel>
                        
                        <StackPanel Spacing="4">
                            <TextBlock Text="数据位:" FontSize="12"/>
                            <ComboBox ItemsSource="{x:Bind ViewModel.AvailableDataBits}"
                                      SelectedItem="{x:Bind ViewModel.DataBits, Mode=TwoWay}"
                                      HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Available Ports -->
                    <StackPanel Spacing="8">
                        <TextBlock Text="可用串口"
                                   Style="{StaticResource SubtitleTextBlockStyle}"
                                   FontWeight="SemiBold"/>
                        
                        <Button Content="🔄 扫描串口"
                                Command="{x:Bind ViewModel.ScanPortsCommand}"
                                HorizontalAlignment="Stretch"/>
                        
                        <Button Content="📂 打开所有串口"
                                Command="{x:Bind ViewModel.OpenAllPortsCommand}"
                                HorizontalAlignment="Stretch"
                                Background="{ThemeResource AccentFillColorDefaultBrush}"/>
                        
                        <ListView x:Name="PortListView"
                                  ItemsSource="{x:Bind ViewModel.AvailablePorts, Mode=OneWay}"
                                  SelectionMode="Single"
                                  MaxHeight="300"
                                  SelectionChanged="PortListView_SelectionChanged">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <Grid>
                                        <TextBlock Text="{x:Bind}" VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>

                    <!-- Open Ports -->
                    <StackPanel Spacing="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0"
                                       Text="已打开串口"
                                       Style="{StaticResource SubtitleTextBlockStyle}"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>
                            
                            <Button Grid.Column="1"
                                    Content="❌"
                                    ToolTipService.ToolTip="关闭所有串口"
                                    Command="{x:Bind ViewModel.CloseAllPortsCommand}"
                                    FontSize="14"
                                    Padding="8,4"/>
                        </Grid>
                        
                        <ListView x:Name="OpenPortListView"
                                  ItemsSource="{x:Bind ViewModel.OpenPorts, Mode=OneWay}"
                                  SelectionMode="Single"
                                  Height="200">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 颜色指示器和选择器 -->
                                        <DropDownButton Grid.Column="0"
                                                        Margin="0,0,8,0"
                                                        Padding="4"
                                                        VerticalAlignment="Center"
                                                        ToolTipService.ToolTip="选择端口颜色">
                                            <Border Width="20" Height="20"
                                                    CornerRadius="2"
                                                    Background="{Binding ColorHex, Converter={StaticResource HexColorToBrushConverter}}"/>
                                            <DropDownButton.Flyout>
                                                <MenuFlyout Placement="Bottom">
                                                    <MenuFlyoutItem Click="PortColorMenuItem_Click" Tag="#107C10"><MenuFlyoutItem.Icon><FontIcon Glyph="&#xE91F;" Foreground="#107C10"/></MenuFlyoutItem.Icon>绿色</MenuFlyoutItem>
                                                    <MenuFlyoutItem Click="PortColorMenuItem_Click" Tag="#0078D4"><MenuFlyoutItem.Icon><FontIcon Glyph="&#xE91F;" Foreground="#0078D4"/></MenuFlyoutItem.Icon>蓝色</MenuFlyoutItem>
                                                    <MenuFlyoutItem Click="PortColorMenuItem_Click" Tag="#E74856"><MenuFlyoutItem.Icon><FontIcon Glyph="&#xE91F;" Foreground="#E74856"/></MenuFlyoutItem.Icon>红色</MenuFlyoutItem>
                                                    <MenuFlyoutItem Click="PortColorMenuItem_Click" Tag="#FF8C00"><MenuFlyoutItem.Icon><FontIcon Glyph="&#xE91F;" Foreground="#FF8C00"/></MenuFlyoutItem.Icon>橙色</MenuFlyoutItem>
                                                    <MenuFlyoutItem Click="PortColorMenuItem_Click" Tag="#881798"><MenuFlyoutItem.Icon><FontIcon Glyph="&#xE91F;" Foreground="#881798"/></MenuFlyoutItem.Icon>紫色</MenuFlyoutItem>
                                                    <MenuFlyoutItem Click="PortColorMenuItem_Click" Tag="#00B7C3"><MenuFlyoutItem.Icon><FontIcon Glyph="&#xE91F;" Foreground="#00B7C3"/></MenuFlyoutItem.Icon>青色</MenuFlyoutItem>
                                                    <MenuFlyoutItem Click="PortColorMenuItem_Click" Tag="#C239B3"><MenuFlyoutItem.Icon><FontIcon Glyph="&#xE91F;" Foreground="#C239B3"/></MenuFlyoutItem.Icon>粉色</MenuFlyoutItem>
                                                    <MenuFlyoutItem Click="PortColorMenuItem_Click" Tag="#498205"><MenuFlyoutItem.Icon><FontIcon Glyph="&#xE91F;" Foreground="#498205"/></MenuFlyoutItem.Icon>橄榄绿</MenuFlyoutItem>
                                                    <MenuFlyoutItem Click="PortColorMenuItem_Click" Tag="#8764B8"><MenuFlyoutItem.Icon><FontIcon Glyph="&#xE91F;" Foreground="#8764B8"/></MenuFlyoutItem.Icon>淡紫</MenuFlyoutItem>
                                                    <MenuFlyoutItem Click="PortColorMenuItem_Click" Tag="#CA5010"><MenuFlyoutItem.Icon><FontIcon Glyph="&#xE91F;" Foreground="#CA5010"/></MenuFlyoutItem.Icon>棕橙</MenuFlyoutItem>
                                                </MenuFlyout>
                                            </DropDownButton.Flyout>
                                        </DropDownButton>

                                        <StackPanel Grid.Column="1" Spacing="4">
                                            <TextBlock Text="{Binding PortName}"
                                                       FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding StatisticsDisplay, Mode=OneWay}"
                                                       FontSize="12"
                                                       Opacity="0.7"/>
                                        </StackPanel>

                                        <Button Grid.Column="2"
                                                Content="❌"
                                                FontSize="12"
                                                Padding="4"
                                                VerticalAlignment="Center"
                                                Click="ClosePort_Click"
                                                Tag="{Binding PortName}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- Main Content Area -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Baud Rate Detection Alert -->
                <Border x:Name="BaudRateAlertBorder"
                        Grid.Row="0"
                        Background="#FFF3CD"
                        BorderBrush="#FFEEA4"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="12,8"
                        Margin="8,8,8,4"
                        Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0"
                                   Text="⚠️"
                                   FontSize="16"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        
                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock x:Name="BaudRateAlertTitle"
                                       Text="检测到波特率可能不匹配"
                                       FontWeight="SemiBold"
                                       FontSize="14"/>
                            <TextBlock x:Name="BaudRateAlertMessage"
                                       Text="建议切换到正确的波特率以获得更好的数据质量"
                                       FontSize="12"
                                       Opacity="0.8"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2"
                                    Orientation="Horizontal"
                                    Spacing="8"
                                    VerticalAlignment="Center">
                            <Button x:Name="AutoFixBaudRateButton"
                                    Content="自动修复"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Click="AutoFixBaudRate_Click"
                                    FontSize="12"/>
                            <Button x:Name="DismissAlertButton"
                                    Content="忽略"
                                    Click="DismissAlert_Click"
                                    FontSize="12"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Search and Controls -->
                <StackPanel Grid.Row="0" Padding="8" Spacing="8" Margin="8,4,8,8">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <StackPanel Spacing="4">
                            <Grid Width="350">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ComboBox x:Name="SearchBox"
                                         Grid.Column="0"
                                         PlaceholderText="正则表达式搜索..."
                                         IsEditable="True"
                                         Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}"
                                         ItemsSource="{x:Bind ViewModel.RecentSearchTexts, Mode=OneWay}"
                                         SelectionChanged="SearchBox_SelectionChanged"
                                         DropDownClosed="SearchBox_DropDownClosed"
                                         LostFocus="SearchBox_LostFocus"
                                         KeyDown="SearchBox_KeyDown"
                                         HorizontalAlignment="Stretch"
                                         MaxDropDownHeight="200">
                                    <ComboBox.ItemContainerStyle>
                                        <Style TargetType="ComboBoxItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="MinWidth" Value="320"/>
                                        </Style>
                                    </ComboBox.ItemContainerStyle>
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate x:DataType="x:String">
                                            <Grid Margin="0,4" HorizontalAlignment="Stretch">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                          Text="{x:Bind}"
                                                          VerticalAlignment="Center"
                                                          Margin="0,0,8,0"
                                                          TextWrapping="NoWrap"
                                                          TextTrimming="CharacterEllipsis"
                                                          ToolTipService.ToolTip="{x:Bind}"/>

                                                <Button Grid.Column="1"
                                                       Content="🗑️"
                                                       FontSize="12"
                                                       Padding="6,4"
                                                       MinWidth="28"
                                                       VerticalAlignment="Center"
                                                       Click="DeleteSearchHistory_Click"
                                                       Tag="{x:Bind}"
                                                       ToolTipService.ToolTip="删除此历史记录"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>

                                <Button Grid.Column="1"
                                       x:Name="ClearSearchTextButton"
                                       Content="✕"
                                       ToolTipService.ToolTip="清空当前搜索"
                                       Click="ClearSearchText_Click"
                                       Padding="8,4"
                                       Margin="4,0,0,0"
                                       VerticalAlignment="Center">
                                    <Button.Visibility>
                                        <Binding Path="ViewModel.HasSearchText" Mode="OneWay">
                                            <Binding.Converter>
                                                <StaticResource ResourceKey="BoolToVisibilityConverter"/>
                                            </Binding.Converter>
                                        </Binding>
                                    </Button.Visibility>
                                </Button>

                                <Button Grid.Column="2"
                                       Content="🗑️"
                                       ToolTipService.ToolTip="清空搜索历史"
                                       Click="ClearSearchHistory_Click"
                                       Padding="8,4"
                                       Margin="4,0,0,0"
                                       VerticalAlignment="Center"/>
                            </Grid>
                            
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock x:Name="RegexErrorTextBlock"
                                           Foreground="#C42B1C"
                                           FontSize="11"
                                           Visibility="Collapsed"/>
                                
                                <TextBlock x:Name="MatchCountTextBlock"
                                           FontSize="11"
                                           Opacity="0.7"
                                           Visibility="Collapsed"/>
                            </StackPanel>
                        </StackPanel>
                        
                        <Button Content="全选"
                                Click="SelectAllLogs_Click"/>
                        
                        <Button Content="🗑️ 清空日志"
                                Command="{x:Bind ViewModel.ClearLogsCommand}"
                                ToolTipService.ToolTip="清空所有日志记录"/>
                        
                        <CheckBox Content="自动滚动"
                                  IsChecked="{x:Bind ViewModel.AutoScroll, Mode=TwoWay}"
                                  x:Name="AutoScrollCheckBox"/>
                    </StackPanel>
                </StackPanel>

                <!-- Logs Display - Using ItemsRepeater with Virtualization for High Performance -->
                <ScrollViewer Grid.Row="1"
                              x:Name="LogScrollViewer"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto"
                              Background="{ThemeResource LayerFillColorDefaultBrush}"
                              Padding="8"
                              IsTabStop="True"
                              TabNavigation="Local">
                    <ItemsRepeater x:Name="LogsItemsRepeater"
                                   ItemsSource="{x:Bind ViewModel.DisplayLogs, Mode=OneWay}"
                                   VerticalAlignment="Top">
                        <ItemsRepeater.Layout>
                            <!-- StackLayout with better spacing for readability -->
                            <StackLayout Orientation="Vertical" Spacing="2"/>
                        </ItemsRepeater.Layout>
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate>
                                <!-- Larger font and better spacing for readability -->
                                <TextBlock Text="{Binding FormattedText}"
                                           Foreground="{Binding ColorHex, Converter={StaticResource HexColorToBrushConverter}}"
                                           FontFamily="Consolas"
                                           FontSize="14"
                                           TextWrapping="NoWrap"
                                           Padding="2,3,2,3"
                                           IsTextSelectionEnabled="True"/>
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </ScrollViewer>

                <!-- Send Area -->
                <StackPanel Grid.Row="2"
                            Spacing="8"
                            Padding="8"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Orientation="Horizontal" Spacing="16">
                        <CheckBox Content="十六进制发送"
                                  IsChecked="{x:Bind ViewModel.SendAsHex, Mode=TwoWay}"/>
                        <CheckBox Content="显示发送数据"
                                  IsChecked="{x:Bind ViewModel.ShowSentData, Mode=TwoWay}"/>
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock Text="TX颜色:" VerticalAlignment="Center"/>
                            <ComboBox x:Name="TxColorComboBox"
                                      SelectedValue="{x:Bind ViewModel.TxColorHex, Mode=TwoWay}"
                                      SelectedValuePath="Tag"
                                      Width="100">
                                <ComboBoxItem Tag="#0078D4"><StackPanel Orientation="Horizontal"><Rectangle Width="16" Height="16" Fill="#0078D4" Margin="0,0,8,0"/><TextBlock Text="蓝色"/></StackPanel></ComboBoxItem>
                                <ComboBoxItem Tag="#E74856"><StackPanel Orientation="Horizontal"><Rectangle Width="16" Height="16" Fill="#E74856" Margin="0,0,8,0"/><TextBlock Text="红色"/></StackPanel></ComboBoxItem>
                                <ComboBoxItem Tag="#FF8C00"><StackPanel Orientation="Horizontal"><Rectangle Width="16" Height="16" Fill="#FF8C00" Margin="0,0,8,0"/><TextBlock Text="橙色"/></StackPanel></ComboBoxItem>
                                <ComboBoxItem Tag="#881798"><StackPanel Orientation="Horizontal"><Rectangle Width="16" Height="16" Fill="#881798" Margin="0,0,8,0"/><TextBlock Text="紫色"/></StackPanel></ComboBoxItem>
                            </ComboBox>
                        </StackPanel>
                        <TextBlock Text="(RX颜色在已打开串口列表中设置)"
                                   VerticalAlignment="Center"
                                   FontSize="11"
                                   Opacity="0.6"/>
                    </StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="SendTextBox"
                                 Grid.Column="0"
                                 Text="{x:Bind ViewModel.SendText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 PlaceholderText="输入要发送的数据..."
                                 Margin="0,0,8,0"/>

                        <Button Grid.Column="1"
                                Content="发送"
                                Style="{StaticResource AccentButtonStyle}"
                                Click="SendButton_Click"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Grid Grid.Row="2"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              Padding="8,4"
              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
              BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0"
                       Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                       VerticalAlignment="Center"/>
            
            <TextBlock Grid.Column="1"
                       Text="{x:Bind ViewModel.VersionDisplay, Mode=OneWay}"
                       VerticalAlignment="Center"
                       Opacity="0.7"
                       FontSize="11"
                       Margin="16,0,0,0"/>
        </Grid>
    </Grid>
</Window>