# CLAUDE.md

The agent / developer guide for this repository lives in **[AGENTS.md](./AGENTS.md)**.

Please read `AGENTS.md` before making any change. It is the single source of truth for:

- the mandatory **documentation sync requirement** (code changes must update the related docs),
- build / run / publish commands,
- architecture, DI registrations, threading model and performance-critical components,
- reliability mechanisms that must not be regressed,
- version management, CI/CD and release process.

Do not duplicate content here — update `AGENTS.md` instead.
